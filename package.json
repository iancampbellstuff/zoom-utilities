{
  "name": "zoom-utilities",
  "version": "1.0.0",
  "description": "An app for simplifying Zoom tasks.",
  "productName": "Zoom Utilities",
  "author": "iancampbellstuff",
  "license": "ISC",
  "private": true,
  "workspaces": [
    "client",
    "common",
    "server"
  ],
  "engines": {
    "node": "20.x",
    "npm": ">=10.x"
  },
  "scripts": {
    "preinstall-all": "npm ci",
    "install-all": "npx lerna run ci --loglevel debug --scope=@zoom-utilities/{client,common,server}",
    "start": "npx lerna run start --stream --scope=@zoom-utilities/{client,server}",
    "test": "npx lerna run test --stream --scope=@zoom-utilities/{client,common,server}"
  },
  "devDependencies": {
    "husky": "^9.1.5",
    "lerna": "^8.1.8"
  },
  "husky": {
    "hooks": {
      "pre-push": "npm test"
    }
  }
}
