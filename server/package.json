{
  "name": "@zoom-utilities/server",
  "description": "An Express app for simplifying Zoom tasks.",
  "productName": "Zoom Utilities Server",
  "author": "icampbell2",
  "license": "ISC",
  "private": true,
  "scripts": {
    "build": "echo \"Error: no command specified\" && exit 1",
    "clean": "npx --no-install rimraf dist",
    "lint-fix": "npm run lint -- --fix",
    "lint": "eslint --ext .js,.ts ./",
    "prebuild": "npm run lint && npm run clean",
    "pretest": "npm run lint && npx --no-install rimraf coverage",
    "start": "echo \"Error: no command specified\" && exit 1",
    "test-only": "jest --colors",
    "test-watch": "npm test -- --watchAll",
    "test": "npm run test-only"
  },
  "devDependencies": {
    "@types/jest": "26.0.23",
    "@types/node": "16.0.0",
    "@typescript-eslint/eslint-plugin": "4.28.1",
    "@typescript-eslint/parser": "4.28.1",
    "eslint": "7.30.0",
    "eslint-config-prettier": "8.3.0",
    "eslint-plugin-jest": "24.3.6",
    "jest": "27.0.6",
    "ts-jest": "27.0.3",
    "typescript": "4.3.5"
  }
}
